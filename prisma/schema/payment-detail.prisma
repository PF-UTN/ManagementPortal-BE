model PaymentDetail {
  id            Int         @id @default(autoincrement())
  order         Order?      @relation("OrderToPaymentDetail")
  paymentType   PaymentType @relation(fields: [paymentTypeId], references: [id])
  paymentTypeId Int
}

model Bill {
    id             Int          @id @default(autoincrement())
    beforeTaxPrice Decimal      @db.Decimal(12, 2)
    totalPrice     Decimal      @db.Decimal(12, 2)
    order          Order        @relation(fields: [orderId], references: [id])
    orderId        Int          @unique
    billItems      BillItem[]
}


model VehicleUsage {
    id        Int      @id @default(autoincrement())
    date      DateTime
    odometer  Decimal
    kmUsed    Decimal
    vehicle   Vehicle  @relation(fields: [vehicleId], references: [id])
    vehicleId Int
}
